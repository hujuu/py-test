apply plugin: 'groovy'

// In this section you declare where to find the dependencies of your project
repositories {
	jcenter()
}

dependencies {
	//testCompile 'org.codehaus.groovy:groovy-all:2.5.0'
	testCompile "org.codehaus.groovy:groovy:2.4.12"
	testCompile 'junit:junit:4.12'
	testCompile 'org.seleniumhq.selenium:selenium-java:3.5.1'
	testCompile 'org.gebish:geb-core:1.1'
	testCompile 'org.gebish:geb-junit4:1.1'
	// This dependency is exported to consumers, that is to say found on their compile classpath.
	//api 'org.apache.commons:commons-math3:3.6.1'
	// This dependency is used internally, and not exposed to consumers on their own compile classpath.
	implementation 'com.google.guava:guava:21.0'
	// Use JUnit test framework
	testImplementation 'junit:junit:4.12'
}
